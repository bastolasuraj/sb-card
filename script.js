document.addEventListener("DOMContentLoaded", () => {
    const workExperience = document.querySelector(".work-experience");
    const education = document.querySelector(".education");
    
    workExperience.addEventListener("click", () => {
        // Show lightbox pop effect for work experience
        // You can implement this using CSS and JavaScript libraries like Lightbox or Modal.
    });
    
    education.addEventListener("click", () => {
        // Show lightbox pop effect for education
    });
});
